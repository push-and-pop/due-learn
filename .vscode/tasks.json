{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "go", 
            "args": [
                "build",
                "-o",
                "${workspaceFolder}/bin/due",
                "."
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$go"],
            "detail": "构建项目并输出到 bin 目录"
        }
    ]
}